
> **NOTE**
>
> The physics list can be selected using the option
> ```
> -phys_list <value>
> ```
> where `<value>` can be a combination of the Geant4 physics constructors separated by the `+` sign. For example
> ```
> -phys_list="FTFP_BERT + G4NeutronCrossSectionXS"
> ```
{: .doc-important }
